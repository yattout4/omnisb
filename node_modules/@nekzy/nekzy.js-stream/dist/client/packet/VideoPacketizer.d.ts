/// <reference types="node" />
import { VoiceUdp } from "../voice/VoiceUdp";
import { BaseMediaPacketizer } from "./BaseMediaPacketizer";
/**
 * VP8 payload format
 *
 */
export declare class VideoPacketizer extends BaseMediaPacketizer {
    private _pictureId;
    constructor(connection: VoiceUdp);
    private incrementPictureId;
    createPacket(chunk: any, isLastPacket?: boolean, isFirstPacket?: boolean): Buffer;
    onFrameSent(): void;
    private makeFrame;
}
